// Day-one launch scope. Flip a flag to false to make that section public again;
// its route, nav link and homepage block all return together.
export const HIDE_ARTICLES = true; // (K)Need to Know
export const HIDE_MAP = true;      // Find Good Bread map
